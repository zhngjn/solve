#pragma once
#include <vector>

#include "array.h"

namespace alg {

template <typename T>
void SwimUp(std::vector<T>& a, T k) {

}

template <typename T>
void SwimDown(std::vector<T>& a, T k) {

}

template <typename T>
bool IsHeap(const std::vector<T>& a) {
    return true;
}

} // namespace alg
